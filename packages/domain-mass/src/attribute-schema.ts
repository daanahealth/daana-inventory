// Medication attribute schema for the MASS domain.
// TODO: domain-mass agent fills this in

import type { AttributeSchema } from "@daana-health/inventory-core";

export const medicationSchema: AttributeSchema = {
  type: "object",
  properties: {},
};
